rules_version = '2';

// Firebase Storage Security Rules for Health Records
// TEMPORARY: Open rules for testing - REVERT AFTER TESTING
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
